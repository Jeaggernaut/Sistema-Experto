tecnologia(flutter) :-
    tipo_app(movil),
    tiempo(rapido).

tecnologia(react) :-
    tipo_app(web).

tecnologia(nodejs) :-
    tipo_app(backend),
    tiempo(rapido).

api(graphql) :-
    complejidad(alta).

api(rest) :-
    complejidad(baja).

api(rest) :-
    complejidad(media).